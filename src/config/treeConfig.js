// Feature flags and configuration for the family tree
export const TREE_CONFIG = {
  // If true, only one child can be expanded per level at a time
  // If false, multiple children can be expanded simultaneously
  singleExpandPerLevel: true,
};
