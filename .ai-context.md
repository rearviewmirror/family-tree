# AI Context - Family Tree Project

## Project Overview

**Name**: Family Tree for Nashera Mia Bari  
**Type**: React + Vite web application  
**Purpose**: Interactive visualization of a family tree with support for multiple generations, bilingual content (English & Bengali), and social media links

## Tech Stack

- **Frontend Framework**: React 19 (JSX)
- **Build Tool**: Vite 6.3.1
- **Styling**: Tailwind CSS 3.2 + DaisyUI 5.0
- **CSS Processing**: PostCSS 8.5 + Autoprefixer 10.4
- **Data Parsing**: PapaParse 5.4.1
- **Icons**: Heroicons React 2.2
- **Deployment**: GitHub Pages (gh-pages)
- **Linting**: ESLint 9.22 with React plugins
- **Node Version**: ES Modules (type: module)

## Project Structure

```
/Users/shahbaz/development/personal/family-tree/
├── src/
│   ├── App.jsx                      # Main application component
│   ├── main.jsx                     # React app entry point
│   ├── App.css                      # App-level styles
│   ├── index.css                    # Global styles
│   ├── components/
│   │   └── TreeNode.jsx             # Recursive tree node component with expand/collapse
│   ├── data/
│   │   ├── familyData.js            # CSV parser and tree builder
│   │   └── familyTree.csv           # CSV data file with family members
│   ├── assets/                      # Image/media assets
│   └── styles/
│       └── TreeStyles.css           # Tree-specific CSS (420 lines)
├── index.html                       # HTML entry point
├── package.json                     # Dependencies and npm scripts
├── vite.config.js                   # Vite configuration (base: "/family-tree/")
├── eslint.config.js                 # ESLint rules
├── tailwind.config.cjs              # Tailwind CSS configuration
├── postcss.config.cjs               # PostCSS configuration
├── deploy.sh                        # Bash deployment script for GitHub Pages
├── CNAME                            # GitHub Pages custom domain file
└── README.md                        # Project documentation

```

## Key Features & Implementation

### 1. Family Tree Data Structure

**CSV Format** (`familyTree.csv`):
- Columns: `id`, `parentId`, `name`, `nameBn`, `photo`, `social`
- Loaded as raw text using Vite's `?raw` import
- Parsed with PapaParse with `header: true` and `skipEmptyLines: true`

**Tree Building** (`familyData.js`):
```javascript
- Creates a flat map of nodes
- Links children to parents via parentId
- Returns single root or synthetic root if multiple roots exist
- Each node has: id, name, nameBn, photo, social, children[]
```

### 2. Tree Visualization

**TreeNode Component** (`TreeNode.jsx`):
- Recursive component that renders each family member
- Features:
  - **Expand/Collapse**: Toggle children visibility with chevron buttons
  - **Bilingual Display**: Shows both English name and Bengali name
  - **Social Links**: Validates URLs with regex and renders profile links
  - **Responsive Icons**: Chevron icons from Heroicons (different sizes for mobile/desktop)
  - **Scroll Handling**: Complex logic for mobile vs desktop (see Known Issues)
- Props: `person` object, `isRoot` boolean, `scrollContainerRef` for container reference
- Uses `useState` for expanded state, `useRef` for DOM access, `useEffect` for scroll listeners

**Styling** (`TreeStyles.css`):
- 420 lines of custom CSS
- Uses CSS Grid and Flexbox for layout
- Mobile-responsive with breakpoint at 768px
- Mobile: auto-scrolling on expand, min-width: max-content
- Desktop: zoom: 80%, centered layout, scroll prevention logic
- Connector lines between parent and children nodes

### 3. UI & Styling

- **Tailwind CSS**: Utility-first styling (bg-base-100, text-primary, etc.)
- **DaisyUI**: Pre-built components and theme system
- **Custom CSS**: TreeStyles.css handles complex tree layout and animations
- **Responsive Design**: Mobile-first approach with media queries at 768px
- **Colors & Themes**: Uses DaisyUI color scheme (primary, base-100, base-300, etc.)

### 4. Deployment

**Environment**: GitHub Pages with custom domain  
**Domain**: `nashera-miabari.shahbazkhan.co.uk`

**npm Scripts**:
- `npm run dev` - Start Vite dev server
- `npm run build` - Build for production + add CNAME file
- `npm run preview` - Preview production build
- `npm run lint` - Run ESLint
- `npm run deploy` - Deploy to GitHub Pages (runs build first)

**Deploy Script** (`deploy.sh`):
- Removes old dist folder
- Builds project
- Adds CNAME file
- Runs `npx gh-pages -d dist`

## Known Issues & Code Notes

### 1. **Vite Base Path Configuration** (FIXED)
- **Issue**: Base path was "/" but should be "/family-tree/" for GitHub Pages subpath
- **Fix Applied**: Updated `vite.config.js` to `base: "/family-tree/"`
- **Impact**: Ensures CSS, JS, and asset paths work correctly on GitHub Pages

### 2. **README Merge Conflict** (FIXED)
- **Issue**: Git merge conflict markers (<<<<<<, ======, >>>>>>) in README
- **Fix Applied**: Resolved conflict and created comprehensive documentation
- **Content**: Added features, setup, deployment, project structure, and tech stack info

### 3. **Complex Scroll Prevention Logic in TreeNode**
- **Issue**: TreeNode has excessive scroll-handling code (40+ lines)
  - Multiple setTimeout calls (100ms, 200ms, 300ms)
  - requestAnimationFrame for prevention
  - Platform-specific logic (mobile vs desktop)
- **Current Behavior**: 
  - Desktop: Prevents auto-scrolling on expand (prevents disruptive behavior)
  - Mobile: Enables auto-scrolling to show children
- **Recommendation**: Monitor user feedback; simplify if possible after testing on devices

### 4. **Console Logging in Production**
- **Issue**: `console.log('TreeNode rendering: ...)` on lines 9-15 of TreeNode.jsx
- **Recommendation**: Remove or replace with debug mode check for production

## Data Flow

1. **CSV File** → Loaded as raw text via Vite
2. **PapaParse** → Converts CSV to array of objects
3. **familyData.js** → Builds hierarchical tree structure
4. **App.jsx** → Renders root TreeNode with tree object
5. **TreeNode.jsx** → Recursively renders each person and their children
6. **TreeStyles.css** → Applies visual tree layout and connectors

## Browser Support

- Modern browsers with ES6+ support
- CSS Grid and Flexbox required
- Mobile: iOS Safari 12+, Chrome Mobile 80+
- Desktop: Chrome 80+, Firefox 75+, Safari 13+, Edge 80+

## Development Workflow

1. **Setup**: `npm install`
2. **Development**: `npm run dev` (Vite hot reload on file changes)
3. **Code Quality**: Run `npm run lint` before commits
4. **Build**: `npm run build` (production-ready with CNAME)
5. **Preview**: `npm run preview` (test production build locally)
6. **Deploy**: `./deploy.sh` (full deployment pipeline)

## Important Configuration Files

| File | Purpose |
|------|---------|
| `vite.config.js` | Build config, base path: "/family-tree/" |
| `package.json` | Dependencies, scripts, homepage URL |
| `tailwind.config.cjs` | Tailwind theme customization |
| `postcss.config.cjs` | PostCSS with Tailwind and Autoprefixer |
| `eslint.config.js` | Code quality rules |
| `deploy.sh` | GitHub Pages deployment script |

## CSV Data Structure Example

```csv
id,parentId,name,nameBn,photo,social
1,,Syed Shorafat Hossain,সৈয়দ শরাফাত হোসেন,sharafat.jpg,
2,1,Syed Ekramul Haque,সৈয়দ একরামুল হক,son-a.jpg,
3,2,Syed Moazzem Hossain(Tota Mia),সৈয়দ মোয়াজ্জেম হোসেন(তোতা মিয়া),,
```

## Next Steps for Improvement

1. **Remove debug console.log** from TreeNode component
2. **Test** scroll behavior on actual mobile devices (iOS/Android)
3. **Optimize** TreeNode scroll logic if causing performance issues
4. **Add photo loading** - Currently references photo files but may need lazy loading
5. **Error handling** - Add fallbacks for missing CSV data or malformed records
6. **Analytics** - Optional: Add Google Analytics or similar for visitor tracking
7. **Accessibility** - Review ARIA labels and keyboard navigation for tree nodes
8. **Performance** - Consider virtual scrolling if tree becomes very large (1000+ nodes)

## Deployment Checklist

- [ ] Ensure CNAME domain is set correctly in deploy.sh
- [ ] Verify GitHub repository settings have Pages enabled
- [ ] Confirm domain DNS points to GitHub Pages
- [ ] Test production build with `npm run preview`
- [ ] Run `npm run lint` and fix any issues
- [ ] Execute `./deploy.sh` for deployment
- [ ] Wait 2-5 minutes for GitHub Pages to refresh
- [ ] Verify site is live at custom domain

## Contact & Attribution

**Built by**: Shahbaz Khan  
**Project**: Family Tree for Nashera Mia Bari  
**Last Updated**: December 2025
